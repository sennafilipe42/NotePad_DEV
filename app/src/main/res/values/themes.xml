<resources>
    <!-- Base application theme. -->
    <style name="Theme.NotePad_DEV" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/azul_fraco</item>
        <item name="colorPrimaryVariant">@color/azul_fraco</item>
        <item name="colorOnPrimary">@color/light_mode</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.NotePad_DEV.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.NotePad_DEV.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.NotePad_DEV.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Light Mode-->
    <!-- Background se o modo Light mode estiver ligado-->

    <style name="BACKGROUND_TEMA">
        <item name="android:background">#FFF</item>
    </style>

    <!-- Texto se o modo Light mode estiver ligado-->

    <style name="TEXT_TEMA">
        <item name="android:textColor">#000</item>
    </style>

    <!-- Botão se o modo Light mode estiver ligado-->

    <style name="BOTAO_TEMA">
        <item name="android:background">@android:color/holo_blue_dark</item>
        <item name="android:textColor">#FFF</item>
    </style>

</resources>